<!DOCTYPE html>
<html lang="en">
  <!-- prettier-ignore -->
  <head>
    <%- include('includes/head', { title: post.title }) %>
    <link rel="stylesheet" href="/styles/posts.css" />
  </head>

  <body>
    <%- include('includes/header') %>
    <!-- prettier-ignore -->
    <main id="post-detail">
    <h1><%= post.title %></h1>
    <section id="post-meta">
      By: <%= post.author_name %>
      (<address><a href="mailto:<%= post.author_email %>"><%= post.author_email %></a></address>)
      on <time datetime="<%= post.date.toISOString() %>"> <%= post.date.toLocaleString() %></time>
    </section>
    <hr>
    <section>
      <p id="body"><%= post.body %></p>
    </section>
  </main>
  </body>
</html>
